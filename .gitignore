# python cache
__pycache__/
*.pyc
.paper_text/
results/
models/
.env

# personal project brief - not for publication
INSTRUCTION.md

# --- secrets -----------------------------------------------------------
# This repository is PUBLIC. Never commit credentials, keys or auth files.
# Keep them in ~/.ssh/config, a password manager, or files matched below.
proxyAuth.txt
*.local.sh
*.secret
.env
.env.*
id_rsa*
id_ed25519*
*.pem
*.key

# transient git bundles used to ship commits over SSH when the proxy is down
*.bundle
